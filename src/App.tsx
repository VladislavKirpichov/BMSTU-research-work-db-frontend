import React from 'react'
import Router from './Router'

const App: React.FC = () => {
    return (
        <React.StrictMode>
            <Router />
        </React.StrictMode>
    )
}

export default App
